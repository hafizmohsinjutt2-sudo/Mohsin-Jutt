[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Note: Your /api routes will not work on Netlify without 
# converting server.ts to Netlify Functions or proxying to a 
# different backend service.
